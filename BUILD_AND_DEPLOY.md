# 🚀 ขั้นตอน Build และ Deploy หลังจากสร้าง .env.production

## ขั้นตอนที่ 1: ตรวจสอบไฟล์ .env.production

ตรวจสอบว่าไฟล์ `.env.production` มีเนื้อหาดังนี้:

```env
VITE_API_BASE_URL=https://imageapi.sg8net.com/api/v1
VITE_BACKEND_URL=https://imageapi.sg8net.com
```

**⚠️ สำคัญ**: ต้องเป็น **full URL** (เริ่มด้วย `https://`)

---

## ขั้นตอนที่ 2: ลบโฟลเดอร์ dist (ถ้ามี)

ลบโฟลเดอร์ `dist/` เพื่อให้แน่ใจว่า build ใหม่ทั้งหมด:

### Windows Command Prompt:
```bash
cd C:\Users\silve\project\roiet\fui-furniture
rmdir /s /q dist
```

### Windows PowerShell:
```powershell
cd C:\Users\silve\project\roiet\fui-furniture
Remove-Item -Recurse -Force dist
```

### หรือใช้ File Explorer:
1. เปิดโฟลเดอร์ `C:\Users\silve\project\roiet\fui-furniture`
2. ลบโฟลเดอร์ `dist` (ถ้ามี)

---

## ขั้นตอนที่ 3: Build โปรเจกต์ใหม่

รันคำสั่ง build:

```bash
cd C:\Users\silve\project\roiet\fui-furniture
npm run build
```

**หมายเหตุ**: 
- Build อาจใช้เวลาสักครู่
- ตรวจสอบว่าไม่มี error ใน terminal
- ไฟล์ที่ build จะอยู่ในโฟลเดอร์ `dist/spa/`

---

## ขั้นตอนที่ 4: ตรวจสอบ Build Output

### 4.1 ตรวจสอบว่าโฟลเดอร์ dist/spa ถูกสร้าง

ตรวจสอบว่าโฟลเดอร์ `dist/spa/` มีไฟล์:
- `index.html`
- `assets/` (โฟลเดอร์ที่มีไฟล์ JS, CSS)
- ไฟล์อื่นๆ

### 4.2 ตรวจสอบว่า Environment Variable ถูก embed

1. เปิดไฟล์ใน `dist/spa/assets/*.js` (ไฟล์ JavaScript ที่ build แล้ว)
2. ค้นหา `imageapi.sg8net.com` - ควรเห็น URL ที่ถูกต้อง
3. หรือค้นหา `VITE_API_BASE_URL` - ควรเห็น `https://imageapi.sg8net.com/api/v1`

**ตัวอย่างที่ควรเห็นในไฟล์ JS:**
```javascript
// ควรเห็น URL แบบนี้
"https://imageapi.sg8net.com/api/v1"
// หรือ
VITE_API_BASE_URL:"https://imageapi.sg8net.com/api/v1"
```

---

## ขั้นตอนที่ 5: อัปโหลดไฟล์ไปยัง Server

อัปโหลดไฟล์ทั้งหมดจาก `dist/spa/` ไปยัง server:

### ผ่าน FTP/SFTP:
1. เชื่อมต่อกับ server
2. ไปที่โฟลเดอร์ `public_html/image` (หรือโฟลเดอร์ที่ตั้งค่า Document Root)
3. อัปโหลดไฟล์ทั้งหมดจาก `dist/spa/`:
   - `index.html`
   - โฟลเดอร์ `assets/`
   - ไฟล์อื่นๆ ทั้งหมด

### ผ่าน File Manager:
1. เปิด Hostinger File Manager
2. ไปที่โฟลเดอร์ `public_html/image`
3. อัปโหลดไฟล์ทั้งหมดจาก `dist/spa/`

**⚠️ สำคัญ**: 
- อัปโหลดไฟล์ทั้งหมด (ไม่ใช่โฟลเดอร์ `spa`)
- ไฟล์ `index.html` ต้องอยู่ใน root ของ `public_html/image`
- โฟลเดอร์ `assets/` ต้องอยู่ใน root เช่นกัน

---

## ขั้นตอนที่ 6: ตรวจสอบใน Browser

### 6.1 Clear Browser Cache

1. เปิด Browser
2. กด `Ctrl + Shift + Delete` (Windows) หรือ `Cmd + Shift + Delete` (Mac)
3. เลือก "Cached images and files"
4. Clear cache

หรือ Hard Refresh:
- `Ctrl + Shift + R` (Windows)
- `Cmd + Shift + R` (Mac)

### 6.2 ตรวจสอบ Environment Variable

เปิด Browser Console (F12) และพิมพ์:

```javascript
console.log('API Base URL:', import.meta.env.VITE_API_BASE_URL);
```

**ควรเห็น:**
```
API Base URL: https://imageapi.sg8net.com/api/v1
```

### 6.3 ตรวจสอบ Login Request

1. เปิด Browser Developer Tools (F12)
2. ไปที่ Tab **Network**
3. ลอง Login
4. ตรวจสอบว่า login request ไปที่:
   - ✅ **ถูกต้อง**: `https://imageapi.sg8net.com/api/v1/auth/login`
   - ❌ **ผิด**: `https://image.sg8net.com/api/v1/auth/login`

---

## Checklist

- [ ] ไฟล์ `.env.production` มี `VITE_API_BASE_URL=https://imageapi.sg8net.com/api/v1` (full URL)
- [ ] ลบโฟลเดอร์ `dist/` แล้ว
- [ ] Build โปรเจกต์ใหม่: `npm run build` (ไม่มี error)
- [ ] ตรวจสอบ Build Output ว่ามี `imageapi.sg8net.com` ในไฟล์ JS
- [ ] อัปโหลดไฟล์ใหม่จาก `dist/spa/` ไปยัง server
- [ ] Clear browser cache
- [ ] ตรวจสอบใน Browser Console ว่า `VITE_API_BASE_URL` ถูกต้อง
- [ ] ตรวจสอบใน Network tab ว่า login request ไปที่ Backend domain

---

## ถ้ายังไม่ได้ผล

### ตรวจสอบว่าไฟล์ถูก Build ใหม่จริงๆ

1. ตรวจสอบ timestamp ของไฟล์ใน `dist/spa/assets/`
2. ควรเป็นเวลาที่ build ล่าสุด

### ตรวจสอบว่าไฟล์ถูกอัปโหลดถูกต้อง

1. ตรวจสอบว่าไฟล์ `index.html` อยู่ใน root ของ `public_html/image`
2. ตรวจสอบว่าโฟลเดอร์ `assets/` อยู่ใน root เช่นกัน

### ตรวจสอบว่า Environment Variable ถูก embed

1. เปิดไฟล์ JS ใน `dist/spa/assets/` (หรือดูใน Browser Network tab)
2. ค้นหา `imageapi.sg8net.com` - ควรเห็น URL ที่ถูกต้อง

---

## หมายเหตุ

- Environment variables ใน Vite จะถูก embed ใน **build time**
- ถ้าเปลี่ยน `.env.production` ต้อง **build ใหม่** ทุกครั้ง
- ตรวจสอบว่าไฟล์ที่อัปโหลดไปยัง server เป็นไฟล์ที่ build ใหม่จริงๆ

