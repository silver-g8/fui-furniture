# 📝 วิธีสร้างไฟล์ .env.production

## ปัญหา
ไฟล์ `.env.production` ไม่มีอยู่ในโปรเจกต์ ทำให้ `VITE_API_BASE_URL` ไม่ถูกตั้งค่า และ login request ไปที่ Frontend domain แทน Backend domain

## วิธีแก้ไข

### ขั้นตอนที่ 1: สร้างไฟล์ `.env.production`

สร้างไฟล์ใหม่ชื่อ `.env.production` ในโฟลเดอร์ `fui-furniture` (root ของโปรเจกต์)

**ตำแหน่งไฟล์:**
```
fui-furniture/
├── .env.production  ← สร้างไฟล์นี้ที่นี่
├── package.json
├── quasar.config.ts
└── src/
```

### ขั้นตอนที่ 2: เพิ่มเนื้อหาในไฟล์

เปิดไฟล์ `.env.production` และเพิ่มเนื้อหาดังนี้:

```env
# Production Environment Variables
# ⚠️ สำคัญ: ต้องเป็น full URL (เริ่มด้วย https://)

# API Base URL - ตั้งค่าเป็น URL ของ Backend API
VITE_API_BASE_URL=https://imageapi.sg8net.com/api/v1

# Backend URL - สำหรับ CSRF cookie endpoint (optional)
VITE_BACKEND_URL=https://imageapi.sg8net.com
```

### ขั้นตอนที่ 3: ตรวจสอบว่าไฟล์ถูกสร้างถูกต้อง

ตรวจสอบว่า:
- ✅ ไฟล์ชื่อ `.env.production` (มีจุดนำหน้า)
- ✅ อยู่ในโฟลเดอร์ `fui-furniture` (root ของโปรเจกต์)
- ✅ มี `VITE_API_BASE_URL=https://imageapi.sg8net.com/api/v1` (full URL)

### ขั้นตอนที่ 4: Build โปรเจกต์ใหม่

```bash
cd C:\Users\silve\project\roiet\fui-furniture

# ลบโฟลเดอร์ dist (ถ้ามี)
rmdir /s /q dist

# Build ใหม่
npm run build
```

### ขั้นตอนที่ 5: ตรวจสอบ Build Output

1. เปิดไฟล์ `dist/spa/assets/*.js` (ไฟล์ JavaScript ที่ build แล้ว)
2. ค้นหา `imageapi.sg8net.com` - ควรเห็น URL ที่ถูกต้อง

หรือตรวจสอบใน Browser Console (หลังจาก deploy):

```javascript
console.log('API Base URL:', import.meta.env.VITE_API_BASE_URL);
```

ควรเห็น: `https://imageapi.sg8net.com/api/v1`

### ขั้นตอนที่ 6: อัปโหลดไฟล์ใหม่

อัปโหลดไฟล์ทั้งหมดจาก `dist/spa/` ไปยัง server

## หมายเหตุ

- ไฟล์ `.env.production` อาจจะถูก ignore โดย `.gitignore` (ปกติแล้วควร ignore)
- Environment variables ใน Vite จะถูก embed ใน **build time**
- ถ้าเปลี่ยน `.env.production` ต้อง **build ใหม่** ทุกครั้ง
- ตรวจสอบว่าไฟล์ `.env.production` อยู่ในโฟลเดอร์ `fui-furniture` (root ของโปรเจกต์)

## วิธีสร้างไฟล์ใน Windows

### วิธีที่ 1: ใช้ Notepad

1. เปิด Notepad
2. คัดลอกเนื้อหาจาก `.env.production.example` หรือพิมพ์ตามตัวอย่างด้านบน
3. บันทึกเป็น `.env.production` (ต้องใส่ชื่อเต็ม `.env.production` ใน Notepad)
4. เลือก "All Files" ใน Save as type
5. บันทึกในโฟลเดอร์ `fui-furniture`

### วิธีที่ 2: ใช้ Command Line

```bash
cd C:\Users\silve\project\roiet\fui-furniture

# คัดลอกจากไฟล์ตัวอย่าง
copy .env.production.example .env.production

# หรือสร้างใหม่
echo VITE_API_BASE_URL=https://imageapi.sg8net.com/api/v1 > .env.production
echo VITE_BACKEND_URL=https://imageapi.sg8net.com >> .env.production
```

### วิธีที่ 3: ใช้ VS Code หรือ Editor อื่น

1. เปิด VS Code ในโฟลเดอร์ `fui-furniture`
2. สร้างไฟล์ใหม่ชื่อ `.env.production`
3. เพิ่มเนื้อหาตามตัวอย่างด้านบน
4. บันทึกไฟล์

