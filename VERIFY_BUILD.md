# 🔍 วิธีตรวจสอบว่า Build ถูกต้อง

## ปัญหา

Login request ยังไปที่ Frontend domain (`image.sg8net.com`) แทน Backend domain (`imageapi.sg8net.com`)

## วิธีตรวจสอบ

### วิธีที่ 1: ตรวจสอบไฟล์ Build Output (แนะนำ)

1. เปิดไฟล์ใน `dist/spa/assets/` (ไฟล์ JavaScript ที่ build แล้ว)
2. ค้นหา `imageapi.sg8net.com` - ควรเห็น URL ที่ถูกต้อง
3. หรือค้นหา `VITE_API_BASE_URL` - ควรเห็น `https://imageapi.sg8net.com/api/v1`

**ตัวอย่างที่ควรเห็นในไฟล์ JS:**

```javascript
// ควรเห็น URL แบบนี้
'https://imageapi.sg8net.com/api/v1';
// หรือ
VITE_API_BASE_URL: 'https://imageapi.sg8net.com/api/v1';
// หรือ
baseURL: 'https://imageapi.sg8net.com/api/v1';
```

**ถ้าไม่เห็น `imageapi.sg8net.com`** แสดงว่า:

- ❌ Environment variable ไม่ถูก embed ใน build
- ❌ ไฟล์ `.env.production` ไม่ถูกอ่าน
- ❌ ต้อง build ใหม่

### วิธีที่ 2: ตรวจสอบใน Browser Network Tab

1. เปิด Browser Developer Tools (F12)
2. ไปที่ Tab **Network**
3. ลอง Login
4. ดู Request URL:
   - ✅ **ถูกต้อง**: `https://imageapi.sg8net.com/api/v1/auth/login`
   - ❌ **ผิด**: `https://image.sg8net.com/api/v1/auth/login`

### วิธีที่ 3: ตรวจสอบไฟล์ที่อัปโหลดไปยัง Server

1. ตรวจสอบว่าไฟล์ที่อัปโหลดไปยัง server เป็นไฟล์ที่ build ใหม่จริงๆ
2. ตรวจสอบ timestamp ของไฟล์
3. ตรวจสอบว่าไฟล์ `index.html` และ `assets/*.js` เป็นไฟล์ล่าสุด

### วิธีที่ 4: ตรวจสอบใน Source Code (ถ้าเข้าถึงได้)

1. เปิดไฟล์ JS บน server (ผ่าน browser หรือ FTP)
2. ค้นหา `imageapi.sg8net.com` - ควรเห็น URL ที่ถูกต้อง

## ถ้ายังไม่เห็น `imageapi.sg8net.com` ใน Build Output

### ขั้นตอนแก้ไข:

1. **ตรวจสอบไฟล์ `.env.production`**:

   ```bash
   cd C:\Users\silve\project\roiet\fui-furniture
   type .env.production
   ```

   ควรเห็น:

   ```
   VITE_API_BASE_URL=https://imageapi.sg8net.com/api/v1
   VITE_BACKEND_URL=https://imageapi.sg8net.com
   ```

2. **ตรวจสอบว่าไฟล์อยู่ในตำแหน่งที่ถูกต้อง**:
   - ไฟล์ต้องอยู่ใน: `C:\Users\silve\project\roiet\fui-furniture\.env.production`
   - ไม่ใช่ใน `src/` หรือโฟลเดอร์อื่น

3. **ลบโฟลเดอร์ dist และ Build ใหม่**:

   ```bash
   cd C:\Users\silve\project\roiet\fui-furniture
   rmdir /s /q dist
   npm run build
   ```

4. **ตรวจสอบ Build Output อีกครั้ง**:
   - เปิดไฟล์ `dist/spa/assets/*.js`
   - ค้นหา `imageapi.sg8net.com`
   - ควรเห็น URL ที่ถูกต้อง

5. **อัปโหลดไฟล์ใหม่**:
   - อัปโหลดไฟล์ทั้งหมดจาก `dist/spa/` ไปยัง server
   - ตรวจสอบว่าไฟล์ถูกอัปโหลดถูกต้อง

## หมายเหตุ

- Error `import.meta may only appear in a module` ใน Browser Console เป็นเรื่องปกติ - ไม่ต้องกังวล
- วิธีตรวจสอบที่ถูกต้องคือดูใน Network tab หรือไฟล์ build output
- Environment variables ใน Vite จะถูก embed ใน **build time** เท่านั้น
